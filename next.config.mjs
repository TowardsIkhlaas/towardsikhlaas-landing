/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/towardsikhlaas-landing",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
